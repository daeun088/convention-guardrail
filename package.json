{
  "name": "convention-guardrail",
  "version": "0.1.0",
  "description": "Catches architectural convention violations at AI code-generation time via an MCP server, with a standalone CLI fallback.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "guardrail": "./dist/cli/index.js"
  },
  "engines": {
    "node": "^20.11.0 || >=21.2.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "ts-morph": "^28.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^20.14.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^2.1.4"
  }
}
