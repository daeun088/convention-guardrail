// TODO(Phase 1, CLI issue): implement `guardrail check <path>` — load config, run the
// static engine, print violations with picocolors (file:line, ruleId,
// message), exit non-zero if any violation is found.
export {};
