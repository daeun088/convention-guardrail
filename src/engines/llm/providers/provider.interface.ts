// TODO(Phase 3): define ProviderInterface (e.g. a single method that sends
// a judgment prompt + context and returns structured violations). Adding a
// provider should mean one new file implementing this interface, zero
// changes elsewhere.
export {};
