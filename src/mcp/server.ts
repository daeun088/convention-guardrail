// TODO(Phase 4): expose the static and LLM engines as MCP tools via
// @modelcontextprotocol/sdk, so an AI coding assistant can call them
// mid-generation. Skeleton only for now — do not implement ahead of
// Phase 4 per .claude/CLAUD.md.
export {};
