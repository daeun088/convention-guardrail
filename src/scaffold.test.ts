import { describe, expect, it } from 'vitest';

// Placeholder sanity test so `pnpm test` has something to run on the empty
// Phase 1 scaffold. Superseded by real unit tests as each module lands
// (config loader, layer-direction rule, CLI — later Phase 1 issues).
describe('scaffold', () => {
  it('runs under vitest', () => {
    expect(true).toBe(true);
  });
});
